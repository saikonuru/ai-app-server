services:
  - type: web
    name: ai-app-server
    env: node
    buildCommand: |
      runtime: node@22
      bun install
      bunx prisma generate
      bunx prisma migrate deploy
    startCommand: bun run start
